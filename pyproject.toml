[project]
name = "llm-internals"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tarun Krisha", email = "krishnatarun7@gmail.com" }
]
requires-python = ">=3.11"

dependencies = [
    "torch==2.2.2",
    "torchvision==0.17.2",
]

[project.scripts]
llm-internals = "llm_internals:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
